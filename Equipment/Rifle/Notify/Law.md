# Rifle Animation Notify Law

- 当前通知放在角色动画资产上 从网格持有角色取得当前装备
- 只将卸下弹匣 装入弹匣与换弹结束转为步枪固定输入
- 通知不得直接修改弹药 换弹标记 黑板或播放蒙太奇
- 没有有效步枪时不提交 镜像装备不通过本地通知产生玩法结果
- 通知的 INDEX_NONE 表示交由输入包处理当前换弹 不能代替实际动作序号向角色回报
- NotifyState 结束只提交结束输入 是否仍需取消由对应输入包判定

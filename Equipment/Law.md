# Equipment Law

严格遵守 `Constitution/Data/Law.md` 与 `Constitution/Equipment/Law.md`

物理目录固定为

- `Catalog/` 保存装备静态Actor配置的检索入口
- `Base/` 保存装备Actor基座 配置类型 动画实例与输入槽值类型
- `Instance/X/` 下保存一种具体装备实现

- Base 禁止持有具体步枪业务 网络载体只识别持有关系与传输边界
- 本次仅保留当前实际使用的入口 禁止占位拓展

# Equipment Template Law

- 直属根只保存 `ABBBEquipment` 公共演员实现与本 Law
- `Definition/` 保存公共静态配置与对外动画事实的数据定义
- `Animation/` 保存每件装备网格使用的动画实例基类
- `Input/` 保存输入存储复用的纯值类型
- 基座负责公共组件和引用的初始化 不负责具体武器输入解析
- 固定行为通过虚函数交给具体装备 禁止把具体武器判断放入基座
- 调用入口必须校验传入的镜像身份 不得由装备自行推断网络身份
- 初始化只向具体装备交接一次 禁止增加通用 Tick 转发入口

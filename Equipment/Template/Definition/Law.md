# Equipment Definition Law

- 本目录只定义公共静态配置和动画读取的事实类型
- 数据定义不为占位创建 cpp 不包含行为处理器或运行时规则
- 配置可以共享 禁止写入单件装备的可变状态
- `FBBBEquipmentAnimationFacts` 是动画读取的结果投影 不能作为玩法状态的第二写入源
- 动画事实只能描述基本事实 禁止加入开火是否允许等二级决策
- 向角色提交的数据包必须定义在角色侧 禁止在此重新定义角色协议

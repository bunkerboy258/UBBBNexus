# Character Config Law

- 每个配置字段需要写中文注释 说明作用含义
- 注释需要声明在引擎内可见

## 目录职责
- `Aim/` 存放角色瞄准配置
- `Animation/` 存放角色动画配置
- `Equipment/` 存放角色装备配置
- `Locomotion/` 存放角色移动配置
- `Network/` 存放角色网络配置
- 禁止在 `Character/Config/` 新增直属文件夹或文件
